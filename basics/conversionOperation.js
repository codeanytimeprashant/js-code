let score="33abc"

console.log(typeof score)

let valueInNumber= Number(score)

console.log(typeof valueInNumber);
console.log(valueInNumber);

// conversion of string into number datatype but the string which is 33abc cannot be converted into number 
// therefore it value is NaN but its type is converted into number but not its value.

let bollean=1

let isbolleantrue= Boolean(bollean)

console.log(isbolleantrue);

// 1 = true; 0 = false;
// "" = false
// "hello" = true

let value =3
let negval=-value
console.log(negval)
